# Assets folder

Assets folder is storing:
- prepared / created running configuration and analysis `exepriments`  
- peptides data for test runs, and experiments (`peptides_data`)
  > Remark:
  > To download whole peptide base run `#TODO - skrypt do pobierani peptydów z GDrive`
- folder with helper scripts `scripts` 
- folder with independent tools used in production `tools`

